import { BaseRepository } from './base/BaseRepository';
import { User } from '../entities/user';

export class UserRepository extends BaseRepository<User>{
    // CreateNewUser(email, username, password): Promise<boolean> {
    //     this.create()
    // }
}